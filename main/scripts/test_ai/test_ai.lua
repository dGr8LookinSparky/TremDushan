
require "lib/helper"
require "lib/ai"

--create new ai
function TestAISpawn( parentNum )

	--parent = entity.FindNumber(parentNum);

    	--ai = AI_Spawn("TestAI", parent:GetOrigin())
	--if not (ai == 0) then
	--	Printf("hello new ai: [%s]", ai:GetClassName() )
	--else
	--	Printf("can't spawn ai!" )
	--end
		
end

